# 三维知识库

三维知识库首版已合并至 main；阅读改进在 `codex/reading-choreography` 分支开发，阶段性提交。GitHub Pages 只在 main 推送时部署。

## 模块边界

- Hugo：Markdown、专栏继承、文章页面和 `knowledge-index.json` 内容契约。
- `knowledge/src/catalog.ts`：内容契约校验与全文检索。
- `knowledge/src/navigation.ts`：真实文章与专栏、选择记忆、子专栏筛选。无 DOM / Three.js 依赖。
- `knowledge/src/rhine/`：来自 RhineLabUI 的模型渲染、材质、灯光与时间轴；适配范围记在来源说明中。
- `app.ts` / `selection-view.ts` / `boot-controller.ts`：场景生命周期、选中文章的展示及开场控制。
- `features.ts`：协调搜索、阅读和设置。
- `reader.ts` / `dialog.ts`：同源文章阅读、模态焦点和退出操作。Hugo 页末发出就绪消息，避免等外部公式、图片和评论资源全部完成才显示正文。
- `reader-outline.ts` / `reader-layout.css`：从真实正文提取二至四级标题，目录定位和滚动高亮，支持收起目录及铺满浏览器窗口的全屏阅读。
- `routes.ts`：场景链接及浏览器历史。正文内换篇与关闭阅读分别处理，避免关闭时误回上一篇文章。
- `search.ts` / `settings.ts` / `preferences.ts`：搜索界面、显示设置及可失效的本地偏好存储。
- `column-carousel.ts` / `column-navigation.ts` / `workspace.css`：顶级专栏的循环坐标、拖动吸附和导航布局。当前项与左右各两项居中展示，额外两个淡出位置衔接滚动；渲染按钮最多七个，与专栏总数无关。
- `archive-gesture.ts`：与 DOM 和渲染无关的拖动状态，处理方向锁定、距离累积、点击判定和取消；场景通过 Pointer Events 统一接入鼠标、触摸和笔。
- `site-navigation.ts`：根据 Hugo 提供的真实永久链接解析文章、专栏、标签和普通页面，不推测中文或多级分类的 URL。
- `assets/js/view-mode.js`：手机首次访问优先经典视图（窄屏或中等宽度触屏），自动判断不写入偏好；手动选择优先。手机上的文章场景链接可保留文章上下文跳到经典正文。
- `assets/js/knowledge-preload.js` / `knowledge-preload.html`：经典页面空闲时按当前构建清单预热同源 HTTP 缓存，不执行三维模块。手机只预热脚本和样式，桌面包含字体、档案模型；省流量、慢速连接和嵌入正文跳过。离开页面取消请求，失败不阻塞阅读。
- `mobile.css` / `responsive.ts`：窄屏使用实际 CSS 像素重新布局；三项专栏导航、触控按钮、完整窗口阅读与可收起目录。默认降低触屏画质，但保留手动设置。三维资源尚未就绪或启动动画播放时，独立的经典视图入口始终可用。
- `assets/js/view-mode.js` / `knowledge-mode.html`：两种视图共享的轻量切换逻辑；Hugo 为脚本生成内容散列。经典页面不加载 Three.js。
- `reading-flow.ts` / `transition.ts` / `reading-flow.css`：可取消的展开和重组流程、过渡输入隔离和正文面板展开。`rhine/assembly.ts` 定义阅读动画所用的六组装配结构。
- `rhine/reading-shot.ts`：同一条可逆时间轴控制档案抬升、居中、开盖、抽纸和纸张贴屏；复用既有 GLB，纸张由 Three.js 创建，无需 Blender 或新增下载资产。

## 构建

需要 Node.js 24、pnpm 11.19.0、Hugo Extended 0.165.0。

```sh
pnpm install --frozen-lockfile
pnpm test
pnpm build
hugo --minify
pnpm check:site
```

本地预览先执行 `pnpm build`，再执行 `hugo server -D`。前端开发时另开终端运行 `pnpm watch`，Hugo 会监测 `static/knowledge` 的产物变动。

前端产物由 Vite 生成到 `static/knowledge`，构建清单同步写入 `data/knowledge.json` 让 Hugo 监测并引用最新散列文件名；两者都不提交。Vite 禁用默认的 `publicDir`，避免把 Hugo 的 `public` 产物递归复制进前端目录。必须先构建前端再构建 Hugo；独立文章和列表仍由原有模板生成。

Three.js 核心、渲染器、博客界面拆成不同缓存块。打开阅读、搜索和设置时暂停主场景绘制，页面进入后台也停止动画帧调度。

## 内容约定

一级专栏对应档案列，子专栏用于筛选；无专栏文章归到 Unfiled。文章 ID 使用已有永久链接，新增文章或调整排序不改变已有 ID。每列数量可以不同；循环的视觉副本不计入文章总数。

文章链接、日期、阅读时长、标签和专栏均来自 Hugo；不复制维护第二份文章。搜索索引包含全文，正文在用户打开阅读器时加载。

索引的 `pages` 字段提供站内页面的 `url/title/kind/term`，用于在场景中处理分类和关于等链接。阅读器把 Hugo 的规范站点地址映射到当前访问地址，兼容本地 `localhost` / `127.0.0.1` 和项目站点路径。

## 操作

- 首页专栏名称与编号、篇数围绕高光中轴居中排列。当前文章直接以大标题、等宽档案编号和折线标注在场景上，不设面板或模糊衬底；篇目计数与翻页控制独立放在左下角。悬停、选中及回到首页有短过渡，标题在连续拖动中淡入更新，不再用黑条遮挡。减少动态效果偏好仍然生效。
- 顶部专栏条支持点击、左右拖动和箭头切列，松手后减速吸附到中央，两侧逐渐虚化淡出。相邻列首尾循环；「全部专栏」下拉框列出每个顶级专栏及篇数，可直接跳转。两种入口与档案集、键盘、搜索定位共享选中状态；每列保留本次访问的文章选择和子专栏筛选。拖动超过 8 像素后不会误触点击；取消、失焦和窗口变化会结束拖动。
- 直接拖动阵列：向左拖进入后续专栏，向上拖翻到后续档案，反向拖动返回。持续拖动可连续翻阅；8 个屏幕像素内的移动视作点击，一旦开始拖动就不会在松手时误选档案。手势锁定主要方向，指针捕获使移出画布仍可完成拖动，取消或失焦会清理手势。
- Enter / 抽取档案进入摘要，展开阅读打开完整正文，Esc 逐层返回。
- 抽取后显示真实文章的「文章概览」。展开阅读约 2200ms、关闭约 1900ms，统一由 `reading-motion.ts` 时间轴驱动，使用主页的 smooth 缓动。镜头环绕贯穿全程，居中、开盖、抽页和靠近屏幕相互重叠。最后 20% 行程中阅读界面直接跟随三维页面的位置、大小和透明度，不再追加独立弹窗动画；关闭时正文淡出与页面后退同时进行。减少动态效果时直接到位，Esc 可取消，结束后恢复按钮焦点。
- 封面围绕左侧边缘开启；页面从开口平直抽出、移向屏幕，关闭时平直退回。`paper-surface.ts` 仅管理空白平面、开口遮挡与接触暗部，不弯曲、不卷纸，不生成文章缩略图或文字纹理。页面贴屏、阅读界面展开完成后，才开始载入正文和播放文字解析。
- 页面采用固定四顶点平面和固定宽高比，开口遮挡用裁切平面实现，不折叠或拉伸顶点；抽页后半段同时向镜头靠近。按屏幕投影大小插值，避免透视引发最后几帧突然放大。
- 每次打开正文（包括缓存重开）播放一次约 850ms 的入场解析。`reader-reveal.ts` 用真实文章的字形矩形测量位置，在 iframe 内的临时 canvas 遮罩上逐字揭示及闪烁乱码，不另排标题和摘要，不修改正文节点。公式、代码和辅助技术读取内容保持完整。效果在正文可用后开始，与网络加载是否命中缓存分开；不会等待外部服务循环播放。滚动、点击、按键、尺寸或字体变化、关闭及换篇都会清理效果，减少动态效果时跳过。
- 详情阶段合并并发装配预取并预编译材质，同一篇文章重复阅读复用装配模型；纸张贴屏后才载入真实正文，避免 iframe 和公式排版与三维运动竞争。模型失败时仍可读正文。浏览器前进、后退和站内链接共用流程，后到的导航会取消旧过渡。
- 阅读动画期间复用阵列矩阵与阴影、暂停景深后处理，由镜头环绕带动整个档案集与主体共同运动，稳定帧不重复绘制；阅读界面使用 transform/opacity 跟随页面。档案拾取使用低面数包围盒代理，不再检测精细 GLB 的全部三角面。
- 顶部统一的「检索」或 `/` 打开全文检索，保留专栏与标签筛选；输入多个词表示同时匹配。选中搜索结果后在阵列中定位，再抽取阅读。
- 独立的 360° 模型查看器已移除；文章摘要中的档案拖动和阅读时的拆解、重组动画保留。
- 首次完成资源载入后播放开场，支持跳过和重播；记录已访问状态。减少动态效果会跳过开场，系统偏好始终优先。
- 阅读器可复制场景链接；独立文章永久链接继续有效。前进/后退可以恢复阅读与摘要状态。
- 三维视图内的标签、专栏、全部文章和关于均通过场景检索或阅读器打开，正文站内链接遵循同一规则；外部网站和许可附件可以在新标签页打开。
- 用「经典视图／三维视图」显式切换并记住偏好。经典首页地址为 `/classic/`；经典页的首页链接始终回到经典首页。文章和关于页切换时保留内容，首页往返恢复最近场景位置。直接打开共享场景链接优先于经典模式偏好。

## 验证

`pnpm test` 覆盖不等长专栏、单篇专栏、首尾循环、筛选、全文搜索、超过 32 篇的列、288 个渲染槽位、场景链接及阅读历史。

第二轮增加：跨列直达后渲染槽位与文章一致、筛选恢复、Unicode 标签和多级专栏解析、两种视图切换与嵌入页不重定向。共 11 项测试。

拖动迭代增加点击抖动、拖动后回原点不误判点击、方向锁定、连续步进、反向拖动与取消手势测试，总计 14 项。浏览器验证横拖 Perception → Robotics、纵拖在 Robotics 内翻篇、拖动后的点击选中及检索入口合并；触屏真机尚未验证。

阅读动效迭代总计 19 项测试：另覆盖新版 GLB 的六组结构、玻璃自上而下揭示、加载中取消不迟到打开正文、阅读后的重组与焦点隔离，以及历史导航取消旧请求。浏览器已验证文章概览、新材质无着色器错误、展开到正文、关闭重组、展开中 Esc 取消后重新打开、浏览器后退与前进、直接阅读链接以及“汇总 → 外推法”换篇后重组回外推法摘要。

取纸阅读迭代共 22 项测试：新增动作先后顺序、纸张在视口内完整显示、贴屏边界与反向姿态恢复，以及动画中途取消不迟到打开正文。浏览器验证全屏阅读、真实目录定位及公式显示；本地分镜页用于检查开盖与抽纸构图，不进入发布源码。

空白页面与解析迭代共 26 项测试：覆盖往返固定顶点与宽高比、无文字纹理、展开结束后才载入文字、取消不迟到显示正文、Unicode 字位测量、缓存重开与旧回调隔离、滚动清理、超时与减少动态效果。浏览器回归页使用真实文章检查首次和二次打开分别触发效果、正文 HTML 及标题位置尺寸不变、效果结束后覆盖层清空；另用分镜页检查开口遮挡和整页平移。验证页仅在本地构建，不进入正式产物。

整体运动迭代共 28 项测试，进一步覆盖动作重叠、阅读界面在三维运动结束前接入、关闭时页面先移动而正文尚未消失、交接端点平滑，以及关闭后镜头位置和朝向准确恢复。

`pnpm check:site` 检查真实 Hugo 产物：所有文章页面和阅读器就绪信号、散列资源、GLB 完整性、原有列表/标签/搜索路由及首页结构化数据。可传入构建目录，例如 `pnpm check:site .verification/production`。

2026-09-09 浏览器验证：原版模型与阵列、切列和单篇列、搜索“牛顿 汇总”、公式阅读、正文内换篇、关闭返回对应档案、浏览器前进/后退、直接阅读链接、模型拆解/重组/退出、子专栏筛选、画质和减少动态效果设置。

第二轮浏览器验证：七列导航和布局、Systems Integration 跨列直达、Robotics 子专栏 3 篇筛选往返恢复、正文“动力学”标签打开 5 篇筛选、场景内关于、对应经典页面往返、经典首页样式和站点标识返回经典首页。生产构建与 `/blog/` 子目录构建均通过产物检查。

移动端入口与布局迭代共 36 项测试，新增首次访问分流、手动视图偏好、文章直达和空闲缓存预热的网络策略验证。浏览器验证 390×844 竖屏、844×390 横屏、移动端默认经典与手动切回三维、加载中切换、全屏阅读、目录收起与长公式不撑宽页面；桌面档案抽取、阅读和经典视图切换回归通过。尚未进行手机真机验证。

MiSans 两个原始字重合计约 9.94 MB；新版两个 GLB 合计约 6.91 MB（旧版 2.72 MB），装配模型在抽取详情后加载。MathJax 和评论仍沿用博客现有的外部服务；它们失败时不阻止正文显示。WebGL 初始化失败或未启用 JavaScript 时提供普通文章入口。
